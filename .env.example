# ============================================
# Clinical Trial Assistant - Environment Variables
# ============================================

# ---------------------------------------------
# OpenAI Configuration
# ---------------------------------------------
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1

# ---------------------------------------------
# LangSmith Configuration (Optional - for monitoring)
# ---------------------------------------------
LANGCHAIN_API_KEY=your_langsmith_key_here
LANGCHAIN_PROJECT=clinical-trial-matching
LANGCHAIN_TRACING_V2=true

# ---------------------------------------------
# PostgreSQL Configuration
# ---------------------------------------------
# When running locally (not in Docker):
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DATABASE=aact
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres

# When running in Docker (override in docker-compose.yml):
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_DATABASE=aact
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres

# ---------------------------------------------
# Elasticsearch Configuration
# ---------------------------------------------
# When running locally (not in Docker):
# ELASTICSEARCH_URL=http://localhost:9200
# ES_INDEX_NAME=aact

# When running in Docker (override in docker-compose.yml):
# ELASTICSEARCH_URL=http://elasticsearch:9200
# ES_INDEX_NAME=aact

# ---------------------------------------------
# API Settings
# ---------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000
STREAMING_ENABLED=false

# ---------------------------------------------
# LLM Settings
# ---------------------------------------------
LLM_MODEL=gpt-4o-mini
TEMPERATURE=0.0

# ---------------------------------------------
# Chainlit Settings (Future UI)
# ---------------------------------------------
# CHAINLIT_HOST=0.0.0.0
# CHAINLIT_PORT=8000
